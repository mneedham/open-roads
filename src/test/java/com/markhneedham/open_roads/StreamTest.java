package com.markhneedham.open_roads;

import java.util.function.Supplier;
import java.util.stream.Stream;

import org.junit.Test;

import org.neo4j.helpers.collection.Pair;

public class StreamTest
{
    @Test
    public void shouldCrossProduct() throws Exception
    {
        // given
//        Stream<Integer> stream1 = Stream.of( 1, 2, 3, 4 );
//        Stream<Integer> stream2 = Stream.of( 5, 6, 7, 8 );
//
//        Supplier<Stream<Integer>> stream1Supplier = () -> stream1;
//        Supplier<Stream<Integer>> stream2Supplier = () -> stream2;
//
//        Stream<Pair<Integer, Integer>> pairStream = stream1Supplier.get().flatMap( s1 -> stream2Supplier.get().map( s2 -> Pair.of( s1, s2 ) ) );
//
//        pairStream.forEach( ( pair ) ->
//        {
//            System.out.println( "p1 = " + pair.first() + ", p2 = " + pair.other() );
//        } );

        // when

        // then
    }
}
